export default async function authMiddleware(to,from){
    // try {
        // const isAdmin = true
        console.log(to)
        console.log(from)

        // if (is auth)
        // return isAdmin
        return true
}
