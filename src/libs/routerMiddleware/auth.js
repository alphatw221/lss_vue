export const authMiddleware = async (to, from) => {
    // try {

        console.log(to)
        console.log(from)

        // if (is auth)
        return {path:to.path}
    // } catch (err) {
    //     console.log(err);
    //     throw  {
    //             path: '/'
    //         }

    // }
};
